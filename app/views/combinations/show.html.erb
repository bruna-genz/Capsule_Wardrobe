<div class="container-fluid full-page">
    <div class="row h-100" id="body-row">
        <%= render "posts/sidebar" %>
        <div class="col pt-3">
            <h1 class="title-small mt-1 mb-4 ml-4"><%= @combination.name %></h1>
            <% if @items.empty? %>
                <h1 class="paragraph-big">No items in this combination yet</h1>
            <% else %>
                <ul id="images-container">
                    <% @items.each do |item| %>
                        <li>
                            <div style='background-image: url(<%=(url_for(item.picture)) %>)'>
                                <%= link_to "Remove item", post_combination_path(:combination_id => @combination_id, :post_id => item.id), method: :delete,  class:"btn btn-secondary btn-sm",  data: { confirm: 'Are you sure?' } %>
                            </div>
                        </li>
                    <% end %>
                </ul>
            <% end %>
            <%= link_to show_options_path(:combination_id => @combination.id), class:"btn btn-outline-secondary text-body ml-4 mt-4 mb-3" do %>
                Add items
                <img src="https://img.icons8.com/metro/26/000000/plus.png">
            <% end %>
            <%= link_to "Delete look", combination_path(@combination), method: :delete,  class:"btn btn-outline-secondary text-body ml-4 mt-4 mb-3",  data: { confirm: 'Are you sure?' } %>
            <br/>
            <%= link_to "Back", combinations_path, class:"btn btn-outline-secondary text-body ml-4" %>
        </div>
    </div>  
</div>